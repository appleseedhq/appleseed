
surface glass(float ior = 1.5)
{
	float from_ior, to_ior;

	if (backfacing())
	{
		from_ior = ior;
		to_ior = 1.0;
	}
	else
	{
		from_ior = 1.0;
		to_ior = ior;
	}

 	Ci = reflection(N, ior) + refraction(N, from_ior / to_ior);
}
