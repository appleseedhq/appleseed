
surface glass(
	float Kr = 0.5,
	float ior = 1.5)
{
	float from_ior, to_ior;

	if (backfacing())
	{
		from_ior = ior;
		to_ior = 1.0;
	}
	else
	{
		from_ior = 1.0;
		to_ior = ior;
	}

 	Ci = Kr * reflection(N) + (1.0 - Kr) * refraction(N, from_ior, to_ior);
}
