bpy::object archive_frame(const FrameArchiver* frameArchiver, const char* directory)
    {
        char* output = 0;
        if (frameArchiver.archive(directory, &output))
        {
            const bpy::str path(output);
            foundation::free_string(output);
            return path;
        }
        return bpy::object();
    }
