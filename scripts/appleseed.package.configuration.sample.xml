<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- The name of the target platform. This parameter is only used to name the final archive.
         Example values: win64-vs140, linux64-gcc4, mac64-gcc4, etc. -->
    <platform>win64-vs140</platform>

    <!-- The name of the configuration. Binary files will be taken from sandbox/bin/<configuration>/. -->
    <configuration>v140\Ship</configuration>

    <!-- The path to the appleseed installation where binaries files will be taken from. -->
    <appleseed_path>C:\appleseed</appleseed_path>

    <!-- The path to appleseed header files. -->
    <appleseed_headers_path>C:\appleseed\src\appleseed</appleseed_headers_path>

    <!-- The path to the Qt shared libraries. -->
    <qt_runtime_path>C:\qt-everywhere-opensource-src-4.8.6\lib</qt_runtime_path>

    <!-- The path to the platform runtime files (Windows only). -->
    <platform_runtime_path>C:\Program Files (x86)\Microsoft Visual Studio 14.0\VC\redist\x64\Microsoft.VC140.CRT</platform_runtime_path>

    <!-- The directory inside which the final archive file will be created. -->
    <package_output_path>C:\appleseed\archives\releases</package_output_path>
</configuration>
